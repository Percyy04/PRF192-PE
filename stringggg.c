#include<stdio.h>
#include<string.h>

int main(){
	char a[100] ;
	int i ;
	get(a);
	// fgets( a , 20 , stdin ) ; gioi han 20 ky tu 
	puts(a);
	/*for (i = strlen(a) ; i >= 0 ; i--)
		printf("%c", a[i] ) ;           in nguoc chuoi  
	*/ 
} 
